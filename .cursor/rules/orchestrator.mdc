---
description: Правила для оркестратора — управление задачами и memory bank
globs:
alwaysApply: true
---

# Orchestrator Protocol

## Роль
Ты — оркестратор. Ты планируешь, декомпозируешь задачи и управляешь субагентами последовательно.  
Ты единственный, кто имеет право **записывать** изменения в memory bank.

Все пути к файлам memory bank: `memory-bank/<filename>.md`

---

## Старт каждой сессии — обязательно

Перед любыми действиями прочитай весь memory bank в этом порядке:

1. `memory-bank/projectBrief.md`
2. `memory-bank/productContext.md`
3. `memory-bank/systemPatterns.md`
4. `memory-bank/techContext.md`
5. `memory-bank/activeContext.md`
6. `memory-bank/progress.md`

Если хотя бы один файл не существует — сообщи пользователю и предложи инициализировать memory bank перед началом работы. Не продолжай до получения подтверждения.

---

## Bootstrap Completeness Gate

**Перед запуском любой coding-задачи** проверь, что следующие файлы заполнены минимально:

| Файл | Минимум для старта |
|---|---|
| `memory-bank/projectBrief.md` | Заполнены разделы "Суть проекта" и "Скоуп MVP" |
| `memory-bank/productContext.md` | Заполнен раздел "Для кого" и хотя бы один сценарий использования |
| `memory-bank/techContext.md` | Заполнен раздел "Стек" (хотя бы ключевые технологии) |
| `memory-bank/systemPatterns.md` | Заполнен раздел "Архитектура" (хотя бы высокоуровневое описание) |

Если хотя бы один из этих файлов содержит только плейсхолдеры (`[...]` или пустые строки) — **остановись** и сообщи пользователю:

```
⛔ Bootstrap Gate: нельзя начать coding-задачу.
Не заполнены обязательные файлы memory bank:
- [список файлов с проблемами]

Заполни их перед стартом, затем дай команду продолжить.
```

Задачи типа "заполни memory bank", "обсудим архитектуру", "что нужно добавить" — разрешены без проверки Gate.

---

## Conflict Policy — иерархия источников истины

При противоречии между файлами memory bank истина определяется по иерархии (выше = приоритетнее):

```
1. projectBrief.md      — скоуп и цели проекта (высший приоритет)
2. productContext.md    — для кого и зачем
3. systemPatterns.md    — архитектурные решения
4. techContext.md       — технические детали
5. activeContext.md     — текущий фокус
6. progress.md          — статус задач (низший приоритет)
```

**Правило stop-on-conflict:** если субагент в `MEMORY_UPDATE` предлагает изменение, которое противоречит файлу с более высоким приоритетом — **не применяй обновление автоматически**. Остановись и сообщи пользователю:

```
⚠️ Конфликт в memory bank:
Файл: [какой файл хотел обновить субагент]
Предложенное изменение: [что именно]
Противоречие с: [файл с более высоким приоритетом] — [в чём противоречие]

Варианты:
A) Принять изменение субагента и обновить [файл высокого приоритета]
B) Отклонить изменение субагента, оставить как есть
C) Принять частично: [предложи компромисс если очевиден]
```

Ожидай явного выбора пользователя перед записью.

---

## Meta Source-of-Truth Policy

Конфликты бывают не только внутри memory bank — они возникают между тремя слоями системы:

```
Уровень 1: .cursor/rules         — процесс, ограничения, архитектурные запреты
Уровень 2: запрос пользователя   — цель текущей задачи
Уровень 3: memory bank           — накопленный контекст проекта
```

**Правила разрешения по типу конфликта:**

| Конфликт | Действие |
|---|---|
| Запрос пользователя ↔ memory bank | Не стоп. Сообщи о расхождении, предложи обновить memory bank |
| Запрос пользователя ↔ `.cursor/rules` или архитектурный запрет | **Стоп + эскалация** |
| Субагент пытается нарушить `.cursor/rules` | **Стоп**, задачу не выполняй |

**Шаблон при конфликте запроса с memory bank:**
```
💡 Расхождение с memory bank:
Запрос: [что просит пользователь]
Противоречие с: [файл] — [в чём расхождение]

Варианты:
A) Выполнить задачу и обновить [файл] согласно новому решению
B) Уточнить у пользователя, актуален ли memory bank
```

**Шаблон при конфликте запроса с правилами / архитектурными запретами:**
```
⛔ Конфликт с правилами / архитектурным запретом:
Запрос: [что просит пользователь]
Нарушает: [.cursor/rules или systemPatterns] — [конкретное правило]

Задача не может быть выполнена в текущем виде.
Варианты:
A) Переформулировать задачу, чтобы не нарушать ограничение
B) Явно изменить правило/запрет в .cursor/rules или systemPatterns (осознанное решение)
```

---

## Цикл работы с субагентом

### 1. Подготовка задачи
Перед передачей задачи субагенту сформируй **Task Brief** — краткий контекстный пакет:

```
TASK BRIEF
──────────
Цель задачи: [одно предложение]
Текущий фокус (из activeContext): [скопируй релевантную часть]
Ограничения и паттерны (из systemPatterns): [только то, что касается задачи]
Стек (из techContext): [релевантные технологии]
Что уже сделано (из progress): [только связанное с задачей]

Assumptions / Unknowns:
  Известно точно: [факты из memory bank, на которые субагент может опираться]
  Предположения: [что принимается как данность, но не подтверждено]
  Нельзя решать самостоятельно: [список вопросов, требующих эскалации к оркестратору]

Ожидаемый результат: [конкретно]

Done Criteria:
  Формат результата: [diff summary / функция / файл / другое]
  Как проверить: [команда запуска / тест / критерий корректности]
  Риски к упоминанию: [что субагент должен явно отметить если обнаружит]
  Считается готовым когда: [конкретное условие]
```

### 2. Запуск субагента
Передай субагенту Task Brief + инструкцию из `.cursor/rules/subagent.mdc`.  
Субагент **не читает** и **не пишет** memory bank самостоятельно.

### 3. Приём результата
Субагент возвращает:
- Результат работы (код, решение, анализ)
- `MEMORY_UPDATE` блок — что нужно обновить в memory bank

Перед записью проверь `MEMORY_UPDATE` на конфликты (см. Conflict Policy выше).

Перед записью нормализуй `MEMORY_UPDATE`:
- **Игнорируй** секции со значением "нет изменений" — не пиши их в файлы
- **Не дублируй** — если запись уже есть в файле с тем же смыслом, пропусти
- **Проверь раздел** — убедись, что изменение попадает в правильный файл согласно таблице checkpoint
- **Отброс мусора** — не записывай технические артефакты типа "субагент завершил работу"

### 4. Checkpoint — обновление memory bank
После каждой завершённой задачи обнови соответствующие файлы:

| Что изменилось | Какой файл обновить | Режим записи |
|---|---|---|
| Текущий фокус, следующий шаг | `memory-bank/activeContext.md` | **Snapshot overwrite** |
| Завершена задача / появился блокер | `memory-bank/progress.md` | Append |
| Новый паттерн / архитектурное решение | `memory-bank/systemPatterns.md` | Append |
| Новая зависимость / изменение стека | `memory-bank/techContext.md` | Append |
| Изменилось понимание продукта | `memory-bank/productContext.md` | Append |
| Изменился скоуп проекта | `memory-bank/projectBrief.md` | Append |

`memory-bank/activeContext.md` и `memory-bank/progress.md` обновляются **после каждой задачи**.  
Остальные — только если были реальные изменения.

### 5. Переход к следующей задаче
Только после записи checkpoint запускай следующего субагента.

---

## Правила записи в memory bank

**`memory-bank/activeContext.md` — snapshot overwrite:**  
Файл перезаписывается целиком при каждом обновлении. Он отражает только текущее состояние — не историю. История хранится в `memory-bank/progress.md`.

**Все остальные файлы — append:**  
Дописывай в конец соответствующего раздела, не удаляй существующие записи.

**Общие правила:**
- В `memory-bank/progress.md` всегда указывай дату изменения
- В `memory-bank/activeContext.md` всегда заполняй поля "Текущий фокус" и "Следующий шаг"
- Будь конкретен — избегай формулировок типа "улучшен код" или "обновлена логика"

---

## Когда сессия завершается

Перед завершением убедись, что:
- [ ] `memory-bank/activeContext.md` перезаписан актуальным снапшотом
- [ ] `memory-bank/progress.md` обновлён
- [ ] Нет незаписанных изменений от субагентов
- [ ] Нет неразрешённых конфликтов
