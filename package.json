{
  "name": "ntt-preflight",
  "version": "0.1.0",
  "private": true,
  "description": "Sunrise NTT Preflight scaffold baseline",
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": {
    "ntt-preflight": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "smoke:verify": "npm run build && node dist/index.js verify --profile ntt-generic --rpc-url http://localhost:8899 --output ./artifacts",
    "smoke:plan": "npm run build && node dist/index.js plan --profile sunrise-executor --rpc-url http://localhost:8899 --output ./artifacts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
